@model IEnumerable<_2025_2C_ReservaEspectaculoORT.Models.Pelicula>

@{
    ViewData["Title"] = "ListarFunciones";
}

<h1>Funciones disponibles para @ViewBag.Pelicula.Titulo</h1>

@foreach (var funcion in ViewBag.Pelicula.Funciones)
{
  //  if (funcion.Fecha < DateTime.Now.AddDays(7) && funcion.Confirmada && funcion.ButacasDisponibles >= ViewBag.CantButacas)
   // {
        <p>tamo en japon</p>
   // }    
}